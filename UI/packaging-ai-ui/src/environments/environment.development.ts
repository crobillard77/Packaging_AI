export const environment = {
  production: false,
  /** Empty so `/v1` and `/health` hit the Angular proxy → API. */
  apiBaseUrl: '',
  pollIntervalMs: 2000,
  defaultOutputDir: 'D:\\DataShare\\Output',
};
